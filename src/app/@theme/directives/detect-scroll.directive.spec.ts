import { DetectScrollDirective } from './detect-scroll.directive';

describe('DetectScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectScrollDirective();
    expect(directive).toBeTruthy();
  });
});
